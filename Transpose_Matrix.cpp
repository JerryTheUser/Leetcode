#include <iostream>
#include <vector>
using std::cout;
using std::endl;
using std::vector;

class Solution {
public:
    static vector<vector<int>> transpose(vector<vector<int>>& A) {
        int size_i = A.size();
        int size_j = A[0].size();
        vector<vector<int>> ret;
    

        return ret;
    }
};

int main(){

}
